{{ if .Values.green.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-nginx-configs-green
data:
    php_upstream.conf: |
      upstream php {
          server 127.0.0.1:9000;
      }
{{ end }}